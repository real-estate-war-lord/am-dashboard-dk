# P9 — Final QA with fresh eyes, docs, release readiness

You did not build any of this. Act as a demanding reviewer whose bar is "the best analytical dashboard the
owner has ever seen", then fix what you find.

## Scope
1. Run the full gate and the whole AC suite (`--phase-upto P9`). Fix every failure you can.
2. **Visual review**: run `tests/ui_smoke.py --full-page` and look at EVERY screenshot at 1440×900, 1366×768,
   1536×864 (add it to the viewports for this run) and 390×844 with your image tool. Make a list in
   `docs/v3/QA.md` of every visual defect (misalignment, inconsistent spacing/typography, clipped text,
   overlapping legends, orphaned controls, leftover v2.6 UI, empty cards, dead links, wrong breadcrumbs,
   anything that contradicts the spec) and fix them, most visible first. Re-shoot and confirm.
3. **Consistency audit**: the same indicator must show the same label, unit, number format and source on map
   legend, picker, chart panel, Data table, test property and export. Grep for leftover old code paths
   (`indsel`, `data-climate`, `vCompare`, `exportAll`, KEY FIGURES) and delete dead code.
4. **Old links**: verify each redirect in the route table by loading it (listed in PROGRESS.md).
5. **Docs**: CHANGELOG.md new top section `## v3.0 — <date>` (what changed for a reader, removed items, URL
   redirects, known issues, what is deferred: portfolio / multi-link pins, pinned chips, …); README.md
   navigation/URL/export sections updated; `docs/UI_V3.md` (routes table incl. redirects, export schema,
   component list, test commands); bump any visible version string to v3.0.
6. `docs/v3/RELEASE_NOTES_FI.md`: a short Finnish summary for the owner (what to look at first in the morning,
   the 5 biggest changes, anything that failed or was deferred, and the one command to publish).

## ACs (phase P9)
Every MUST AC from earlier phases green; smoke green at 4 viewports; QA.md has no open item marked `blocker`.
