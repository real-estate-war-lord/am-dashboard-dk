# P6 — Test property rebuild (single pin)

## Scope (spec amendments A2/A3 and §5.5′ — the portfolio is NOT in scope)
1. `#property?p=lat,lon[:label]` view built from the P5 `studyRow` component with the pin's finest area as the
   entity (quarter > postal code > municipality, level tag shown), plus pin, radius rings and feature layers on
   the mini-map (Layers ▾ from P4, radius select). Mini-map draggable, `⌖` re-centre, `⤢` full screen.
2. Headline tiles clickable → select indicator; IndicatorPicker + PeriodControl + chips (P3) drive the chart
   panel and the mini-map fill.
3. Input: one paste box (`prop-input`) + Go; a new link **replaces** the pin (one property at a time). Empty
   state (`state-empty`) with the input focused and one example link. Privacy line stays here.
4. Sections as `<details>` (`tp-sec-<name>`): Population outlook, Area profile (all figures), Safety,
   Infrastructure nearby (open by default), Public buildings within ring (group identical rows with a count),
   Schools, Climate, Sources & as of — existing card content moved in, not rewritten. State in `show=`.
5. Header actions: Open on map, Copy link, links to the quarter / postal code / municipality pages,
   OpenStreetMap ↗. Keep the `D.portfolio` "own properties" code path untouched (it is unused without data).
6. Keep the URL codec in `route_core.js` list-capable (`p=a;b`) but render only the first item and write only
   one (note in PROGRESS.md: multi-link map pins = owner's future idea).

## ACs (phase P6)
AC-TP1 (re-check), AC-TP2, AC-TP3, AC-TP5, AC-E1 (read `#properties` as `#property`), AC-MM1/MM2 on the
test property.
