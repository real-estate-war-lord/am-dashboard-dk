# P2 — Navigation, Data section, Compare removal, redirects live

## Scope (spec §3, §5.3, amendments A1/A3)
1. Sidebar: **Map · Data · Charts** (group "Market intelligence") and **Test property** (group "Analysis").
   Remove Market, Pipeline, Compare nav items. Footer keeps the export button (P7 turns it into the menu).
2. **Data** view with tabs `Areas · Projects · National series · Sources` (`data-testid=data-tabs`), hashes
   `#data/areas/<kommune|postnr|kvarter>`, `#data/projects`, `#data/national`, `#data/sources`; `#data` →
   `#data/areas/kommune`.
   - Areas = the old Table (unchanged behaviour for now; picker comes in P3).
   - Projects = the old Pipeline table + filters (`ptype`, `pstatus`) and its CSV button.
   - National series = the old Market rebuilt as a table: Series · Latest · Period · y/y · Source ·
     sparkline (last 5 y). Keep the four headline tiles as a compact row. Remove the four big charts.
   - Sources = the old sources fold as a proper sortable table: Source · Publisher · Tables · As of · Fetched ·
     Licence · Used for · ↗. Empty "Fetched" → build date with a `build` tag (AC-D4).
   Keep internal view ids (`table`, `pipeline`, `market`) if that is simpler — only hashes and labels change.
3. Flip `ROUTE_V3 = true`: new hashes are emitted; every old hash redirects via `history.replaceState`.
4. **Delete Compare** (A1): view, state, handlers, CSS, area-page "Compare" button; `#compare?…` redirects.
5. **Test property route** `#property?p=lat,lon[:label]` (A3) served by the existing analysis view for now;
   `#analysis?a=…&la=…` redirects to it; the in-app links/buttons emit the new hash.
6. Breadcrumbs updated for all views (public-building sheet must show the municipality, not "Macro map" —
   AC-SH3).
7. Update tests/ui_smoke.py ROUTES: add the new hashes, keep the old ones (they now test redirects), replace
   the compare route with a redirect check.

## ACs (phase P2)
AC-D1, AC-D2 (read as **4** nav items: Map, Data, Charts, Test property), AC-D3, AC-D4, AC-SH3, AC-TP1,
AC-TP6, AC-U1 (idempotent hash round-trip for every route that exists now).
